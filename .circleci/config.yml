version: 2
jobs:
   one:
     docker:
       - image: circleci/ruby:2.4.1
     steps:
       - checkout
       - setup_remote_docker
       - run: echo "hello world"

workflows:
  version: 2
  wfone:
    jobs:
      - one
