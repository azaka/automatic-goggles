version: 2
jobs:
   one:
     docker:
       - image: circleci/build-image
     steps:
       - checkout
       - setup_remote_docker
       - run: echo "hello world"

workflows:
  version: 2
  wfone:
    jobs:
      - one
